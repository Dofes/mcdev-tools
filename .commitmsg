feat: 新增地图文件管理功能（重命名、复制、删除、多选）

为侧边栏新增完整的世界存档管理功能：

- 列出 minecraftWorlds 目录下所有基岩版世界存档
- 展示显示名称、文件夹名、最后修改时间和存档大小
- 支持内联重命名，同时更新 levelname.txt 和 level.dat 的 NBT 字段
- 支持复制存档，自动生成文件夹名，用户可自定义显示名称
- 支持单个和批量删除，带模态确认对话框
- 多选模式：按钮切换，支持全选和批量操作
- 搜索和排序（按名称/时间，升序/降序）
- 世界设置区域新增 Tab 页签：「存档管理」和「新建存档」
- 一键加载/切换当前开发世界，自动更新 .mcdev.json
- 所有文件操作均有路径遍历防护
- 完整的中英文国际化支持
- CSS 全部使用 VSCode 主题变量
